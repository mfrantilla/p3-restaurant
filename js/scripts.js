$(document).ready( function() {


	slidr.create('slidr-home', {
        breadcrumbs: true,
        controls: 'corner',
        fade: false,
        keyboard: true,
        overflow: false,
        touch: true,
        transition: 'linear'
    }).start();

});